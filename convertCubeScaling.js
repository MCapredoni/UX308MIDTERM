// Calculate the **volume** of a cube. Start with a variable that contains the **height** of the cube in *meters*, and another variable that references the **height** of the cube in **Cubic_Meters**.

function convertCubeScaling(height) {
    return ( height ** 3);
  }

export {convertCubeScaling}